# Next Session TODO

## ✅ Completed
- Repo isolated to `tugboat-solutions/tugboat-portal` (private)
- `tugboat-Aaron-build` branch deleted from `Tugboat-Solutions-Inc/tugboat-portal`
- Local repo on `main`, remote pointing to `tugboat-solutions/tugboat-portal`

## Still Needed
- **Vercel project**: Set up a separate Vercel project for `tugboat-solutions/tugboat-portal` — its own URL, independent from the dev shop's deployment
- **Backend API**: Get repo access + staging URL from dev team
- **Supabase dev project**: Separate from production
- **`.env.local`**: Dev credentials (intentionally not created yet — waiting on above)
